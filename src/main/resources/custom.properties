# Custom Properties File
# This demonstrates loading additional property files

# Application Info
app.name=Spring Cert Notes
app.version=1.0.0
app.max.users=100
app.timeout=5000

# CORS
app.allowed.origins=http://localhost:3000,http://localhost:8080

# Features
app.feature.enabled=true
